{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "site_db",
            "database": "${workspaceFolder:backend}/site.db"
        }
    ]
}